# Review QuartzControl (neunundzwanzigstes, Stand `main` = `review-2026-10-04`)

Gelesen: `review-2026-10-03..review-2026-10-04` (`901d819`) ohne `docs/REVIEW-2026-10-02.md` und
ohne die Auftragsdatei. Nachgezählt mit `git diff --stat`: 19 Dateien, **+427/−56** — der Auftrag
nennt +415/−56, gezählt, „bevor der Commit existiert, der diese Datei trägt“; die zwölf Zeilen
stecken in ebendiesem Commit (Stand in `CLAUDE.md`, Absatz in `docs/reviews.md`). Die
Commit-Tabelle stimmt mit `git log` überein.

**Der Vorbehalt dieser Runde.** Die Fixes und der Auftrag sind von Opus 5, das Review davor und
dieses von Fable 5.1. Vier Entscheidungen der Runde hatte kein Review vorgeschlagen; die habe ich
zuerst angegriffen, wie erbeten. Ergebnis: **Alle vier tragen**, zwei davon besser, als der Auftrag
weiß — die Kollisionsrechnung repariert mehr als den Chip, und sie ist entgegen dem Auftrag längst
unter Scrollen gemessen, weil die Seite in genau diesen Szenen scrollt. Was ich gefunden habe, sind
vier Ränder, zwei Behauptungen des Auftrags, die nicht stimmen, und ein älterer Fehler eine Datei
weiter, der für die RC-Frage mehr zählt als alles im Diff.

Dieses Review hat nichts am Code geändert und nichts committet; die einzige neue Datei ist diese.

Gemessen wurde an vier Wegen:

1. **Die gebaute App** (`npm run build`), Wegwerf-Profile, eigene Playwright-Skripte,
   `colorScheme: null`, 1470 × 900, `cp -Rc`-Kopie von `navigations-testprojekt`. Alle vier Frames
   des Projekts (drawing, editorial, focus, index), Desktop und Mobil, Tastatur und Maus; Ansagen
   über einen `MutationObserver`, dazu je Tastendruck das `scrollTop` von `<main>`, die
   Platzierungen und das offene Bereichsformular. **Daneben der Stand `review-2026-10-03` als
   zweite gebaute App** (`git archive`, `electron-vite build` im Scratchpad) — jede Drag-Szene, bei
   der es auf vorher/nachher ankommt, lief gegen beide.
2. **Der Dienst mit echtem npm** (11.17.0, das gepinnte, gegen die Registry), Bündel dieses
   Standes, das Geschirr `hr/` der Vorrunden: R2 neu gefahren, dazu R4 (ein Paket in **drei**
   Abschnitten), R5 (R2 ohne Fehlschlag), AB1/AB2 und AB3 (gescheiterter Pop — über eine
   git-Attrappe, die nur `stash pop` scheitern lässt und sonst echtes git ist).
3. **`styleService` als Bündel** für `joinUniqueRules`, die gebaute App für den Knopf
   „Ladereihenfolge neu schreiben“ mit ungespeichertem Entwurf (Zwei-Kopien-Datei der Vorrunde).
4. **Prüfskripte**: `typecheck`, `build`, `check:i18n` (1134 + 184, 67 nicht prüfbar),
   `check:core-update` (14 + 3), `check:semver` (18), `check:handbook` (26),
   `template:example -- --check-sync` (3 Kopien byte-gleich, 782 827 Bytes), `smoke` (40 Aufrufe,
   keine Auffälligkeiten) — alle grün.

Unberührt: `navigations-testprojekt` (HEAD `3dff48b`, keine Stashes, Arbeitsbereich wie vor der
Sitzung — die Änderungen dort tragen mtimes vom 13. bis 16. September), kein Projekt unter
`~/Documents/QuartzProjekte/` trägt nach dem Smoke-Lauf den neuen Marker, im Handbuch-Vault nur
gelesen (`git status` leer).

**Die Einbindung greift** auch in dieser Sitzung: `docs/conventions.md` lag vor dem ersten
Werkzeugaufruf als eigener Block im Kontext.

**Nicht gemessen:** die gepackte App, die VMs, ERESOLVE, ein echter Push, ein Screenreader (nur
die Live-Regionen), helles Schema, das gebaute Handbuch und das PDF, ein Lauf von
`build-example-template.mjs`, ein Fenster schmaler als 1470 px, das Layout-Board (seine
Kollisionsrechnung steht nicht im Diff; gelesen). Der gescheiterte Pop ist an einer Attrappe
gemessen, weil ich keinen Weg gefunden habe, ihn mit echtem git herzustellen — der Wächter davor
(`refs/stash^` = HEAD) lässt kaum einen übrig.

## Kurzfassung

Kein Befund der Stufe Hoch, **keiner der Stufe Mittel, fünf Niedrig**, fünf Punkte nebenbei.

**Die Kollisionsrechnung** (größtes Risiko laut Auftrag): trägt. In jeder Szene, die der Auftrag
als ungemessen nennt — Scrollen, anderer Breakpoint, freie Zellen mit echter Verschiebung,
mehrzellige Kästen, Maus auf dem eigenen Kasten —, sagt und tut der Drag das Richtige. Und sie
repariert etwas, von dem weder Commit noch Auftrag wissen: Am Stand davor zielte
Leertaste · Leertaste bei **5 von 13** platzierten Kästen der Frames „focus“ und „index“ auf eine
*fremde* Zelle (`page-body` auf „Zeile 1, Spalte 5“), nicht auf die eigene; abgelehnt wurde das
nur, weil dort etwas lag. Jetzt: 13 von 13 „blieb an seinem Platz“.

**`isOwnPlace`**: trägt, auch der Maus-Weg ist so, wie der Auftrag ihn beschreibt (vorher öffnet
ein 8-px-Zug das Formular, jetzt nicht). Der eine Rand: „eigener Platz“ ist die linke obere Zelle,
und das Gezogene steht nicht immer auf ihr (Befund 3).

**Der Abbruch-Kanal**: AB1 und AB2 bestätigt, an beiden Seiten der gebauten App. Was die neue
Reihenfolge nicht löst: Es gibt bis zu drei Sätze, und gesagt wird einer (Befund 1).

**Das Paket in zwei Abschnitten**: R2 bestätigt. Der Satz, der im *Normalfall* fällt, nennt es
aber weiter doppelt (Befund 2).

## Befunde

### Niedrig

#### 1. Der Abbruch sagt einen von bis zu drei Sätzen — und bei einem gescheiterten Pop nicht den, der `success: false` begründet

**Stelle:** `abortCoreMerge` in `updateService.ts` (`sentences = [dropped-staged, ...popped.sentences]`),
`GitSync.tsx` und `Updates.tsx` (`output.split('\n')[0]`).

Die Runde hat die Sätze der App vor gits Text gestellt; „die erste Zeile“ ist damit immer ein Satz
der App. Aber der Kanal hat jetzt bis zu drei davon (verworfene vorgemerkte Datei · Pop geglückt
oder gescheitert · älterer Stash-Eintrag), durch Leerzeilen getrennt, und angesagt wird der erste.
Gemessen:

    AB2 (Bündel und gebaute App, Git-Sync und Updates)
      Kasten   „Der Abbruch hat den vorgemerkten Stand … verworfen: notes.txt. …“ ·
               „Das Update ist abgebrochen, und die zurückgelegten Paketeinträge sind wieder
               eingetragen.“ · git status
      Ansage   nur der erste Satz — harmlos, der zweite ist die gute Nachricht
    AB3 (Bündel, `git stash pop` scheitert über eine Attrappe), vorgemerkte notes.txt
      success  false
      Kasten   verworfene Datei · „Die zurückgelegten Paketeinträge ließen sich nicht wieder
               eintragen …“ · gits Fehler
      Ansage   „Cancelling discarded the staged state of these files: notes.txt. …“

In AB3 sagt die Ansage also nichts davon, dass der Abbruch nur halb gelungen ist und die eigenen
Pakete im Stash liegen. Vor dieser Runde stand `updateStashPopFailed` vorn; die Umstellung hat ihn
hinter `updateAbortDroppedStaged` gerückt. Der Satz über einen älteren Eintrag
(`updateStashFitsHead`/`updateStashLeftover`) wird nach einem geglückten Pop nie gesagt — vorher
auch nicht.

Was dagegen spräche, es so zu lassen: Der gescheiterte Pop ist kaum herzustellen (ich brauchte
eine Attrappe), und der Kasten trägt alles. Dagegen: Genau für diesen Kanal ist die Ansage in drei
Runden nachgebessert worden. Richtung, **ungemessen**: Reihenfolge nach Gewicht (gescheitert vor
verworfen vor geglückt), oder der Kanal gibt seine Sätze getrennt von gits Text zurück und der
Renderer sagt alle — am Text allein sind Sätze der App und Absätze von git nicht zu unterscheiden.

#### 2. „Your own packages put back: left-pad, is-odd, is-buffer, is-odd, kind-of“ — der Satz des Normalfalls nennt das Paket weiter doppelt

**Stelle:** `updateService.ts:1558` (`updatePackagesReinstalled`), dazu `:1266` und `:1308`
(`updatePackagesPending`) — alle drei `….map((entry) => entry.name).join(', ')`.

Befund 4 der Vorrunde hat den Status, `putBack` und die zwei Sätze des *Fehlerpfads* behandelt
(`entryLabels`). Der Satz, den jeder Lauf sagt, der durchgeht, ist geblieben. R5 (das Projekt von
R2, kein Fehlschlag, echtes npm):

    success true · „Your own packages put back: left-pad, is-odd, is-buffer, is-odd, kind-of“

R4 (dev + optional + peer): „… left-pad, is-odd, is-buffer, is-odd, is-odd“. Es ist dieselbe
Dopplung, die die Runde auf der Seite gezählt und behoben hat, im häufigeren Satz. `entryLabels`
ist dafür schon da.

Zur Frage des Auftrags nach **drei Abschnitten**, R4 mit Fehlschlag im `--save-peer`-Aufruf: Satz
und Status stimmen („no longer in package.json: is-odd (peerDependencies)“, `putBack` ohne
Dopplung). Lauf 2 endet `success: true`, `upToDate` — und mit `is-odd` in `devDependencies` und
`peerDependencies`, aber **nicht mehr in `optionalDependencies`**: `npm install --save-peer`
nimmt die Zeile dort weg (npm 11.17.0), und der Satz sagt „put back: … is-odd, is-odd, is-odd“
über zwei Zeilen. Das ist npms Regel, und dev + optional + peer ist keine Konstellation, die jemand
absichtlich führt; ich nenne es, weil der Auftrag danach fragt. **Die verschobene Zeile** habe ich
gelesen, nicht gemessen: `moved` deckt je nicht beanspruchtem Abschnitt einen fehlenden Eintrag, in
Listenreihenfolge — bei (dev, peer) und einer Zeile, die nur noch in `dependencies` steht, gilt
`dev` als beantwortet und `peer` als fehlend. Das ist die Antwort, die ich auch gäbe.

#### 3. „Eigener Platz“ ist die linke obere Zelle — das Gezogene steht aber nicht immer auf ihr

**Stelle:** `isOwnPlace` und `collisionDetection` in `FrameBuilder.tsx`; der Chip des
`DragOverlay` (`w-fit`, liegt an der linken oberen Ecke des Kastens).

Beim Aufnehmen gewinnt das kleinste Ziel unter der Mitte des *Chips*, und die liegt rund 30–50 px
rechts der Kastenecke. Ist die erste Spalte schmaler, ist das eine andere Zelle *unter dem eigenen
Kasten* — kein eigener Platz, also ein Drop. Hergestellt an einer Kopie (Frame „drawing“, Desktop,
Spalten 1–3 auf `20px`, `columnGap: 4px`), gebaute App, alt wie neu gleich:

    header, Space · Space       „header aufgenommen, liegt über Zelle Zeile 1, Spalte 2.“ ·
                                „header bei Zelle Zeile 1, Spalte 2 abgelegt.“ — header@1/2
    before-body, page-body      ebenso, je eine Spalte nach rechts

Aufnehmen und Ablegen ohne einen Pfeil verschiebt den Kasten also, und `landing()` kappt dabei die
Spanne (12 → 11 Spalten) ohne ein Wort. Keine Verschlechterung — der Stand davor tat dasselbe —,
aber genau der Fall, den `08d3f15` im Titel führt („A placed area let go on its own cell stays what
it was“). In den vier echten Frames des Projekts tritt er nicht auf: Dort sind die Spaltenabstände
68 px breit, die Chip-Mitte liegt in einer Lücke, und das kleinste Ziel darunter ist `box:<id>`.
Wer ihn trifft: ein 12er-Raster mit kleinem Abstand in einem schmalen Fenster, ein fester schmaler
Randstreifen als erste Spalte, ein langer Bereichsname.

Richtung, **ungemessen**: nicht die Geometrie fragen, sondern die Geste — ein Drag, dessen
`event.delta` null ist, hat nichts bewegt, in jedem Raster (`handleDragEnd` und `onDragEnd` der
Ansagen bekommen beide das Ereignis). Befund 5 hat dieselbe Wurzel.

#### 4. Zwei Behauptungen des Auftrags über die Kollisionsrechnung stimmen nicht — beide zu ihren Ungunsten

**„Gemessen ist das nur … ohne Scrollen.“** Die Seite scrollt in genau den Szenen, die die Runde
gemessen hat. Der Fokus auf dem Griff bringt `<main>` von 0 auf 569 px, und dnd-kits
`KeyboardSensor` scrollt bei jedem Pfeil nach unten, dessen Ziel unter der Mitte des Rollers liegt
(`core.esm.js:1242`, die Klammer ist die *halbe* Höhe):

    drawing    header ↓↓↓↓↓      scrollTop 569 → 660 → 740 (Ende) → 740 …, Zeile 2 bis 5 richtig
    editorial  header ↓↓↓↓       550 → 641 → 721 → 801 → 881, left ↓ 917 → 1028
    index      custom-8 ↑ ↓      781 → 692 → 781, „liegt wieder auf seinem Ausgangsplatz“

In keiner Szene erschien während des weichen Scrollens ein Zwischenziel in der Live-Region — auch
nicht unter `left` im Frame „index“ (4 Zeilen × 3 Spalten), wo die Mitte zwischen zwei Zeilen nur
im Kasten `box:left` liegt. Die Annahme „nach jedem Pfeil liegen die Mitten aufeinander“ hält also
auch dort, wo der Sensor scrollt statt bewegt.

**„Beim Aufnehmen eines platzierten Bereichs gewinnt die Zelle unter seiner Mitte, nicht
`box:<id>` — vorher war es über `closestCenter` dieselbe Zelle.“** Beides nur beim `header` des
ersten Frames. In den echten Frames liegt die Chip-Mitte meist in einem Spaltenabstand, es gewinnt
`box:<id>` (nach dem Aufnehmen ist keine Zelle hervorgehoben), und vorher war es *nicht* dieselbe
Zelle — alle platzierten Kästen der Frames „focus“ und „index“, Space · Space:

    Stand davor   8 × „bei Zelle <eigene> abgelegt“ (und ausgewählt), 5 × auf eine fremde Zelle
                  gezielt: right → „Zeile 1, Spalte 10“, page-body → „Zeile 1, Spalte 5“,
                  before-/page-/after-body (index) → je „Spalte 5“; alle fünf abgelehnt, weil
                  dort etwas lag — auf freiem Feld wäre der Kasten gewandert
    jetzt         13 × „blieb an seinem Platz“, nichts ausgewählt

Der Nachtrag in `docs/decisions/layout-frames.md` („header (platziert) … vorher wie jetzt“) stimmt
für `header` und lässt aus, dass die Regel für fünf andere Kästen etwas repariert. Ein Befund nur
der Form nach: Code und Richtung sind besser als ihre Beschreibung, und die Schwelle von 1 px ist
damit an mehr gemessen, als der Auftrag annimmt.

#### 5. Der erste Pfeil aus einem breiten Kasten geht von der Kastenmitte aus: „links“ wandert nach rechts

**Stelle:** `src/utils/dndKeyboard.ts`, `here` (zwanzigstes Review) — **nicht aus diesem Diff**,
alt wie neu gleich gemessen. Hier, weil der neue Kommentar an `collisionDetection` sagt, für jeden
Schritt sei die Mitte richtig, und sich auf dasselbe „Feld, auf dem man steht“ beruft.

`here` ist das kleinste Ziel unter der Chip-Mitte. Liegt die in einem Spaltenabstand, ist das der
eigene Kasten, und der Schritt beginnt in *dessen* Mitte. Frame „focus“, `page-body` (Spalten
4–9), gebaute App:

    Space · →      „liegt über Zelle Zeile 2, Spalte 7“   — drei Spalten statt einer
    Space · ←      „liegt über Zelle Zeile 2, Spalte 6“   — nach **rechts**
    nach einer echten Verschiebung auf Spalte 7: Space · ← · Space
                   „bei Zelle Zeile 2, Spalte 9 abgelegt“, page-body@2/9, Spanne auf 4 gekappt

Ab dem zweiten Pfeil geht es Zelle für Zelle, und die Ansage nennt jedes Ziel richtig — wer hört
oder hinsieht, korrigiert. Aber „Pfeil links verschiebt nach rechts“ ist für den einen Weg, den
die Tastatur hat, ein schlechter erster Eindruck, und es trifft jeden Kasten, der breiter ist als
sein Chip, in jedem der vier Frames dieses Projekts. Richtung, **ungemessen**: Für einen
platzierten Bereich ist das Feld, auf dem er steht, die Zelle, an der seine Platzierung beginnt —
die Aufrufstelle kennt sie; `here` müsste nur ein `box:`-Ziel nicht als Feld nehmen.

## Die Fragen des Auftrags, soweit sie kein Befund sind

**Ein Frame mit freien Zellen, also ein Drop, der wirklich verschiebt.** Frame „focus“: `right`
per Tastatur in die Ablage (↑ ↑ Space: „… über Zelle Zeile 1, Spalte 10“ · „über header“ · „über
Bereiche, die hier nicht im Raster liegen“ · „abgelegt“), danach der Chip `right` Space · Space
„blieb an seinem Platz“, `page-body` → · Space „bei Zelle Zeile 2, Spalte 7 abgelegt“ und steht
dort, danach Space · Space „blieb an seinem Platz“. Trägt (die Schrittweite ist Befund 5).

**Ein anderer Breakpoint.** Frame „index“, Mobil — mit einer Zeile der Größe `0`, deren Zellen
über `min-h` in die nächste Zeile ragen: 8 von 8 Kästen Space · Space „blieb an seinem Platz“,
`before-body` ↑ ↓ Space und `header` ↓ ↓ ↑ ↑ Space enden auf dem Ausgangsplatz.

**Die Maus auf dem eigenen Kasten.** Wie beschrieben: 8 px gezogen — vorher „bei Zelle Zeile 1,
Spalte 1 abgelegt“ und das Formular `area-header` offen, jetzt „blieb an seinem Platz“ und kein
Formular. 400 px nach rechts *im eigenen Kasten* ebenso (der Zeiger steht dann in einem
Spaltenabstand über `box:header`). Ich halte „ein Drag ohne Wirkung ändert die Auswahl nicht“ für
richtig; ein Klick auf den Kasten wählt weiter aus.

**Das Layout-Board.** `GlobalBoard` steht nicht im Diff, seine Kollisionsrechnung ist unverändert;
`nearestDroppableCoordinates` auch. Gelesen, nicht gemessen.

**`joinUniqueRules` und eine Klammer im Kommentar.** Am Bündel: ausgewogene Klammern im Kommentar
(`/* OFL {siehe} */`) ändern nichts; ein `/* … { … */` zwischen zwei Regeln schaltet das Entdoppeln
für den Rest des Blocks ab (nichts geht verloren); ein `}` im Kommentar *in* einer Regel setzt eine
Leerzeile in den Kommentar. Verlust gibt es nur, wenn zwei *verschiedene* Regeln bis zu einem
solchen `}` wortgleich beginnen und aus zwei Körpern kommen — dann fällt der Kopf der zweiten weg
und das CSS ist kaputt. Die App schreibt in ihre Regeln keine Kommentare; dafür muss jemand von
Hand dieselbe unausgewogene Klammer in zwei Regeln setzen. Kein Befund.

**Der Knopf bei ungespeichertem Entwurf.** Gebaute App: Entwurf getippt, Knopf geklickt — die
Datei hat danach einen Block, der Fehlerkasten ist weg, und das gelbe Band sagt, Speichern würde
die Änderung überschreiben. Speichern tut genau das: Der geschachtelte Block ist wieder da, der
Fehler auch. Das ist die Regel jeder Dateioperation dieser Seite (der Entwurf bleibt, das Band
warnt), nicht die des Knopfs. Zwei Kleinigkeiten dazu: Das Band nennt als Ursache „von einem
anderen Tab geändert (Variablen-Überschreibung oder Font-Import)“, und hier war es ein Klick auf
demselben Reiter; und der Knopf ist nur während der *Prüfung* gesperrt, nicht während er schreibt.

**`nestedImportBlock` fragt die Datei.** Trägt. Der Satz steht damit auch neben einem Sass-Fehler
anderer Ursache, solange die Schachtelung da ist — der Rat stimmt dann trotzdem.

**`entryLabels`, der unübersetzte Abschnittsname.** Richtig so: „peerDependencies“ ist das Wort,
das in der Datei steht, nach der der Satz den Leser schickt.

**`updateStashRestored`.** Trägt. Der Satz sagt „Das Update ist abgebrochen“; denselben Kanal ruft
Git-Sync auch für einen hängenden `quartz sync --pull`. Dort gibt es den Satz nur, wenn zugleich
ein Stash dieser App auf HEAD passt — zu weit hergeholt für einen Befund, aber der einzige Satz des
Kanals, der sagt, *was* abgebrochen wurde.

**Die kleinen.** `?tab=site` und `?tab=customCss` gegen `TAB_ORDER` beider Seiten geprüft; über
`src/` und `scripts/` gesucht, kein weiterer Schlüssel, den es nicht gibt. Vault `084f425` gelesen:
„Konfiguration → Website“ / „Configuration → Site“ sind die Namen aus `de.ts`/`en.ts`; die übrigen
fünf „Einrichtung → Konfiguration“ im Handbuch nennen Gruppe → Seite und stimmen.

## Nebenbei aufgefallen, nicht mit erledigt

1. **Das Band „custom.scss wurde inzwischen von einem anderen Tab geändert“** erscheint auch nach
   einer Aktion desselben Reiters (Reihenfolge, Pfeile, der neue Knopf). Nicht aus diesem Diff.
2. **`landing()` kappt die Spanne beim Ablegen am Rand, ohne es zu sagen** (12 → 11 in Befund 3,
   6 → 4 in Befund 5). Die Ansage nennt die Zelle, nicht, dass der Bereich schmaler geworden ist.
   Nicht aus diesem Diff.
3. **Die Raster-Vorschau zeigt `1fr`-Spalten 5 px breit**, wo nur überspannende Kästen darin
   liegen und der Spaltenabstand des Frames 4rem ist (gemessen: 57 · 57 · 57 · 5 · 5 · 5 · 5 · 5 ·
   5 · 57 · 57 · 57 bei 1122 px). Eine leere Mittelzelle ist dort als Mausziel 5 px breit. Das ist
   die Geometrie hinter den Befunden 4 und 5. Nicht aus diesem Diff.
4. **Der Status nennt als „ausstehend“ auch Zeilen, die dastehen** (R2: `left-pad`, `is-odd`,
   `is-buffer` mit npms Bereich neben dem fehlenden `kind-of`) — `stillMissing` als Aussagesatz,
   der Punkt des vierundzwanzigsten Reviews; der Text der Seite fängt das ab, die Liste nicht.
5. **Der Auftrag zählt +415/−56, `git diff` +427/−56** — erklärt und von ihm selbst angekündigt.

## Ändert das die RC-Antwort?

Getrennt von den Befunden, wie erbeten. **Aus den Commits dieser Runde: nein.** Die zwei
Drag-Änderungen sind die, vor denen der Auftrag am meisten warnt, und sie sind die am breitesten
gemessenen dieser Runde — vier Frames, zwei Breakpoints, Scrollen, Maus, alt gegen neu. Sie
nehmen einer Tastatur-Geste eine Fehlwirkung (fünf von dreizehn Kästen) und fügen keine hinzu, die
ich gefunden hätte.

Was die Antwort *schärft*, ist Befund 5, und der ist älter als die Runde: Wenn der RC die Fassung
sein soll, an der VoiceOver über den Frame-Builder geht — das hatte mein Vorgänger genannt —, dann
ist „Pfeil links wandert nach rechts“ das Erste, was dieser Durchgang findet. Es wäre schade, ihn
dafür zu verbrauchen. Ich würde 5 und 3 vor dem RC beheben (beide haben dieselbe Wurzel: die
Chip-Mitte ist nicht der Platz des Bereichs) und 1 und 2 mitnehmen, weil sie je eine Zeile sind.
Die Liste davor bleibt, wie sie war: die gepackte App je Plattform, ein neu gebautes Handbuch samt
PDF (2.2 und 3.4 haben sich geändert), der Satz zum Marker in den Notizen, und je nachdem, was
„RC“ heißen soll, ein echter Push.

Der Vorbehalt zur Aussagekraft bleibt der der Vorrunde: Es ist die zweite Runde in Folge ohne
mittleren Befund, und beide sind vom selben Modell.

## Form

Wie gehabt. Befund 1 ist am Bündel (AB2, AB3 mit git-Attrappe) und an beiden Seiten der gebauten
App (AB2) gemessen, 2 mit echtem npm (R4, R5), 3 bis 5 an zwei gebauten Apps (dieser Stand und
`review-2026-10-03`); die Richtungen sind ungemessen, wo es dasteht. Szenen, Skripte und Ausgaben
liegen im Scratchpad der Sitzung (`…/245ff92b-…/scratchpad`: `ui/d1.mjs` mit den Szenen `s1` bis
`s10`, `ui/nest2.mjs`, `ui/pop.mjs`, `hr/` für echtes npm samt `scene-r4.sh` und `bin-gitfail/`,
`mk/j1.cjs` und `j2.cjs`, `old/` als gebauter Vorher-Stand, `proj-nav2` mit den schmalen Spalten),
nichts davon im Repo. Wenn eine Behauptung dieses Dokuments nicht trägt, ist das ein Befund wie
jeder andere.
