node_modules/
out/
dist/
.DS_Store
*.log
*.tsbuildinfo

# electron-builder output
release/

# Icon-Arbeitsdatei: das SVG und die PNG-Exporte in build/icon-source/ sind versioniert,
# die Affinity-Datei nicht - sie ist 1,9 MB Binaerformat und wird nur zum Zeichnen gebraucht.
*.af

# Claude: lokal verlinkte Skills (Symlinks mit absoluten Pfaden, nicht teambar)
.claude/skills/firecrawl*
.claude/settings.local.json

# git-Bundle: wird beim Packen geholt (scripts/fetch-git.mjs), 26 MB je Architektur
resources/git/

# Downloaded by scripts/build-example-template.mjs; not part of the repo.
scripts/example-template/.fonts/
